name: parsley

services:

  nginx:
    image: nginx:alpine
    container_name: parsley-nginx
    # user: "${UID:-1000}:${GID:-1000}"
    ports:
      - "80:80"
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./www:/www
